import{NextResponse,type NextRequest}from'next/server';export function middleware(req:NextRequest){const protectedPath=req.nextUrl.pathname.startsWith('/workspace')||req.nextUrl.pathname.startsWith('/onboarding');if(protectedPath&&!req.cookies.get('rolefit_session'))return NextResponse.redirect(new URL('/signin',req.url));return NextResponse.next()}export const config={matcher:['/workspace/:path*','/onboarding/:path*']}
