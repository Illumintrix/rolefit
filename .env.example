# Local mode needs no accounts or keys.
SQLITE_PATH="./data/rolefit.db"
NEXT_PUBLIC_APP_URL="http://localhost:3000"
# Optional. When empty, the deterministic extractor uses the same API contract.
OPENROUTER_API_KEY=""
