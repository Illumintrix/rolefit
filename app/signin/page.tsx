'use client';import{useState}from'react';import Link from'next/link';import{Target}from'lucide-react';export default function Signin(){const[e,setE]=useState('');async function submit(x:React.FormEvent<HTMLFormElement>){x.preventDefault();const f=new FormData(x.currentTarget),r=await fetch('/api/auth/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({email:f.get('email'),password:f.get('password')})});const j=await r.json();r.ok?location.assign(j.onboardingComplete?'/workspace':'/onboarding'):setE(j.error)}return <main className="grid min-h-screen place-items-center bg-[#0b101b] p-6"><form onSubmit={submit} className="w-full max-w-sm rounded-2xl bg-white p-8"><div className="grid h-10 w-10 place-items-center rounded-xl bg-slate-950 text-white"><Target size={19}/></div><h1 className="display mt-6 text-2xl font-bold">Welcome back</h1><p className="mt-2 text-sm text-slate-500">Sign in to your Rolefit workspace.</p>{['email','password'].map(x=><label key={x} className="mt-5 block text-xs font-semibold capitalize">{x}<input required name={x} type={x} className="mt-2 h-11 w-full rounded-lg border px-3 outline-none focus:ring-2 focus:ring-blue-500"/></label>)}<button className="mt-5 h-11 w-full rounded-lg bg-blue-600 text-sm font-semibold text-white">Sign in</button>{e&&<p className="text-xs text-rose-600">{e}</p>}<p className="mt-5 text-center text-xs text-slate-500">New here? <Link href="/signup" className="font-semibold text-blue-600">Create an account</Link></p></form></main>}
