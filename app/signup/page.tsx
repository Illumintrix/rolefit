'use client';import{useState}from'react';import Link from'next/link';import{Target,GraduationCap,BriefcaseBusiness}from'lucide-react';export default function Signup(){const[a,setA]=useState<'student'|'professional'>('professional'),[err,setErr]=useState('');async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget),r=await fetch('/api/auth/register',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({name:f.get('name'),email:f.get('email'),password:f.get('password'),audience:a})});const j=await r.json();r.ok?location.assign('/onboarding'):setErr(j.error)}return <AuthShell title="Create your workspace" sub="Start free. No payment details needed."><form onSubmit={submit}><div className="grid grid-cols-2 gap-2">{[['student','Final-year student',GraduationCap],['professional','Working professional',BriefcaseBusiness]].map(([id,t,I])=>{const Icon=I as any;return <button type="button" onClick={()=>setA(id as any)} key={String(id)} className={`rounded-xl border p-3 text-left text-xs ${a===id?'border-blue-500 bg-blue-50':'bg-white'}`}><Icon size={16}/><b className="mt-2 block">{t as string}</b></button>})}</div><Field name="name" label="Name"/><Field name="email" label="Email" type="email"/><Field name="password" label="Password" type="password"/><button className="mt-5 h-11 w-full rounded-lg bg-blue-600 text-sm font-semibold text-white">Create account</button>{err&&<p className="text-xs text-rose-600">{err}</p>}<p className="mt-5 text-center text-xs text-slate-500">Already have an account? <Link className="font-semibold text-blue-600" href="/signin">Sign in</Link></p></form></AuthShell>}
function Field({name,label,type='text'}:{name:string,label:string,type?:string}){return <label className="mt-4 block text-xs font-semibold">{label}<input required name={name} type={type} className="mt-2 h-11 w-full rounded-lg border px-3 text-sm outline-none focus:ring-2 focus:ring-blue-500"/></label>}function AuthShell({title,sub,children}:{title:string,sub:string,children:React.ReactNode}){return <main className="grid min-h-screen grid-cols-2"><section className="flex items-center justify-center bg-[#0b101b] p-12 text-white"><div className="max-w-md"><Target size={30}/><h1 className="display mt-8 text-4xl font-bold">Applications that sound like you. Because they are.</h1><p className="mt-5 leading-7 text-slate-400">Rolefit strengthens real evidence and blocks claims you cannot back up.</p></div></section><section className="grid place-items-center p-8"><div className="w-full max-w-sm"><h2 className="display text-2xl font-bold">{title}</h2><p className="mt-2 text-sm text-slate-500">{sub}</p><div className="mt-7">{children}</div></div></section></main>}
