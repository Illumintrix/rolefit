import{NextResponse}from'next/server';import{compare}from'bcryptjs';import{localDb}from'../../../../lib/local-db';import{startSession}from'../../../../lib/session';export async function POST(r:Request){const{email,password}=await r.json();const u=localDb.prepare('SELECT id,password_hash,onboarding_complete FROM users WHERE email=?').get(String(email).toLowerCase())as any;if(!u||!await compare(String(password),u.password_hash))return NextResponse.json({error:'Email or password is incorrect.'},{status:401});await startSession(u.id);return NextResponse.json({ok:true,onboardingComplete:!!u.onboarding_complete})}
